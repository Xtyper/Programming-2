#include <stdio.h>

int main()
 {
    float num;
    printf("Enter a number: ");
    scanf("%f", &num);

    if (num < 0)
        printf("Number is negative");
    else if (num > 0)
        printf("Number is positive");
    else 
    printf("invalid input");

    return 0;
}
